import React from "react";
import ReactDOM from "react-dom/client";
import CompSearch from "./CompSearch.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CompSearch />
  </React.StrictMode>
);
